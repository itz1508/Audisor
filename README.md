# Audisor

Audisor is the product repository for the backend-first code-audit and gap-evaluation workflow.

The current migration places the Python backend in `backend/`. A future frontend, demo, and local-AI integration may be added beside it.

The backend migration preserves the existing phase behavior and intentionally does not add an HTTP API, conversation memory, or automatic duplicate cleanup.
